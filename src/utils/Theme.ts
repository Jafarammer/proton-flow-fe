import type { ThemeConfig } from "antd";

export const baseTheme: ThemeConfig = {
  cssVar: {
    prefix: "ant",
    key: "proton-flow",
  },

  token: {
    fontFamily: "Ubuntu, sans-serif",
  },
};
